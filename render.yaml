services:
  - type: web
    name: meal-management-system
    env: docker
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: MEAL_DB_PATH
        value: /data/meal_manager.db
    disk:
      name: meal-db-data
      mountPath: /data
      sizeGB: 1
